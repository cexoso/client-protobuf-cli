{
  "name": "@protobuf-es/grpc-utils",
  "publishConfig": {
    "directory": "dist",
    "linkDirectory": false
  },
  "scripts": {
    "test": "vitest",
    "build": "ts-pkg-build build",
    "prepublishOnly": "npm run build"
  },
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "@cexoso/ts-pkg-build": "workspace: *"
  }
}
